- page_title!('Editando destinatario')

#region-destinatarios.container

  .region
    .region-title
      %h1
        Editando
        %b
          destinatario

  .region
    = render 'form'

  .region
    .admin-controls
      = link_to 'Ver', @destinatario, class: 'btn btn-info'
      = link_to 'Regresar', 'javascript:history.back(-1)', class: 'btn btn-warning'
