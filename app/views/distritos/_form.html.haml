= form_for @distrito, :html => { class: 'form-horizontal', role: 'form' } do |f|
  - if @distrito.errors.any?
    #error_explanation
      %h2= "#{pluralize(@distrito.errors.count, "error")}"
      %ul
        - @distrito.errors.full_messages.each do |msg|
          %li= msg

  .form-group
    = f.label :nombre, class: 'col-sm-2 control-label'
    .col-sm-10
      = f.text_field :nombre, class: 'form-control'
  .form-group
    = f.label :provincia_id, class: 'col-sm-2 control-label'
    .col-sm-10
      = f.number_field :provincia_id, class: 'form-control'
  .actions
    = f.submit 'Guardar', class: 'btn btn-default'
