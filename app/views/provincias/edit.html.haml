- page_title!('Editando provincia')

#region-provincias.container

  .region
    .region-title
      %h1
        Editando
        %b
          provincia

  .region
    = render 'form'

  .region
    .admin-controls
      = link_to 'Ver', @provincia, class: 'btn btn-info'
      = link_to 'Regresar', 'javascript:history.back(-1)', class: 'btn btn-warning'
