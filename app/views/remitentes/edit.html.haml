- page_title!('Editando remitente')

#region-remitentes.container

  .region
    .region-title
      %h1
        Editando
        %b
          remitente

  .region
    = render 'form'

  .region
    .admin-controls
      = link_to 'Ver', @remitente, class: 'btn btn-info'
      = link_to 'Regresar', 'javascript:history.back(-1)', class: 'btn btn-warning'
